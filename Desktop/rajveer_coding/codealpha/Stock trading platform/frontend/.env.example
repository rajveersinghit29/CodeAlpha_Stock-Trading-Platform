# Copy this file to .env.local for local overrides (never commit .env.local)
# Set this on Vercel dashboard → Settings → Environment Variables

# Your deployed backend URL (no trailing slash)
# Example: https://your-backend.railway.app
# Leave empty for local dev (Vite proxy handles /api → localhost:8080)
VITE_BACKEND_URL=
